process {
    tag = { "$sid" }
    publishDir = {"./results/"}
    scratch = true
    stageInMode = 'copy'
    stageOutMode = 'rsync'
}

params {
    nb_threads=4
    atlas_utils_folder=/FS_BN_GL_utils/
    brainstem_structures=false
}